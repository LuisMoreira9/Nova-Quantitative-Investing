"""Student strategy package.

Each strategy file should define one or more classes with an ``on_bar(bar)``
method. ``core/main_executor.py`` discovers those classes automatically and
routes any emitted signals through ``core/risk_gateway.py``.
"""
