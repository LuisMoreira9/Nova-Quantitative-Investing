# Official Alpaca Python SDK used for paper trading, stock market data streams,
# latest-price checks, and order submission.
alpaca-py>=0.42.0

# Loads local ".env" files during development so members do not need to export
# environment variables manually before running python -m core.main_executor.
python-dotenv>=1.0.0

# Local dashboard for the connected Alpaca paper account.
streamlit>=1.41.0
pandas>=2.2.0
plotly>=5.24.0
